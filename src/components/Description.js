const Description = ({description})=>{
    return(
        // This is my Description item component
        <p className="w-full text-sm max-w-[70%]">{description}</p>
    )
}

export default Description